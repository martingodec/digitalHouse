<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Película</title>
</head>
<body>
    <form action="/detalle/editar/<%= pelicula.id %> " method="POST">
    <div>
        <label for="">Titulo</label>
        <input type="text" name="titulo" id="titulo" value="<%= pelicula.title %> ">
    </div>
    <div>
        <label for="">Rating</label>
        <input type="text" name="rating" id="rating"  value="<%= pelicula.rating %> ">
    </div>
    <div>
        <label for="">Premios</label>
        <input type="text" name="premios" id="premios"  value="<%= pelicula.awards %> ">
    </div>
    <div>
        <label for="">Duracion</label>
        <input type="text" name="duracion" id="duracion"  value="<%= pelicula.length %> ">
    </div>
    <div>
        <label for="">Fecha de estreno</label>
        <input type="date" name="release_date" id="release_date" value="<%= pelicula.release_date %> ">
    </div>
    
    <div>
        <label for="">Genero</label>
        <select name="genero">
           <% for (let i=0; i< generos.length; i++) { %> 
            <% if (generos[i].id == pelicula.genre_id) { %> 

                <option value="<%= generos[i].id %> " selected>  <%= generos[i].name %>  </option>
                <% } else { %>  
                    
                    <option value="<%= generos[i].id %> "> <%= generos[i].name %>  </option>
                    <% } %> 
           
            
            <% } %> 
        </select>
    </div>

    <button type="submit">Editar</button>
</form>
</body>
</html>